spring:
  #  datasource:
  #    url: jdbc:postgresql://localhost:5432/dronazone
  #    username: dronazone
  #    password: dronazone
  datasource:
    url: jdbc:h2:mem:testdb
    username: dronazone
    password: dronazone
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    topic:
      drone-commands: drone-commands
      drone-status: drone-status
      drone-delivery-update: drone-delivery-update
      order-delivered: order-delivered
      order-packed: order-packed
      order-soon: order-soon
      order-cancelled: order-cancelled
    consumer:
      group-id: test