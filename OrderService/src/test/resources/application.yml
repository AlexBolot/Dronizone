spring:
  #  datasource:
  #    url: jdbc:postgresql://localhost:5432/dronazone
  #    username: dronazone
  #    password: dronazone
  datasource:
    url: jdbc:h2:mem:testdb
    username: dronazone
    password: dronazone
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    topic:
      order-create: order-create
      order-soon: order-soon
      order-cancelled: order-cancelled
      order-delivered: order-delievered

    consumer:
      group-id: test